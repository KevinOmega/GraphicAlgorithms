import React from 'react'
import { useGlobalContext } from './context'

const Algorithms = () => {
    const {matrix,setMatrix} = useGlobalContext();

    
  return (
    <></>
  )
}

export default Algorithms
